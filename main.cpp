#include <iostream>

using namespace std;

void minMax(int, int)
int main()
{
    cout << "Press Ctrl-C to stop" << endl;

    int num;
    while (true)
    {
        for (int i = 1; i <= 5; i++)
        {
            cout << "Enter a number: "
            cin >> num;
        }
        minMax(int &m, int &M);
    }

    return 0;
}

void minMax(int m, int M)
{

    if (i == 1)
    {
        M = num, m = num;
        continue;
    }
}
